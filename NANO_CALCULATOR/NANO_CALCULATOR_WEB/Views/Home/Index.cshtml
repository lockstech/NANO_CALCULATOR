@{
    ViewBag.Title = "NANO_CALCULATOR_WEB_VERSION";
}

<div class="row">
    <div class="col-md-4">
        <h2>NANO_CALCULATOR_WEB_VERSION</h2>
        <p>
            @using (Html.BeginForm("Index", "Home", "Post"))
            {
                <input type="number" name="firstNumber" value="0" />
                <input type="number" name="secondNumber" value="0" />
                <select type="number" name="op" value="1">
                    @for (int i = 0; i < ViewBag.Operation.Length; i++)
                    {
                        <option value="@ViewBag.Operation[i].Value">
                            @ViewBag.Operation[i].Text
                        </option>

                    }
                </select>
                <input type="submit" value="вычислить" />
            <p>@ViewBag.Result</p>
            <img src="~/static/images/rusflag.jpg" alt="флаг РОССИИ" height="500" width="1000">
        }

            </p>
        </div>
    </div>